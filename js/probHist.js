function probabilityHistogram(data, name, prob, result, bin, w, h){
  

  this.binsize = 1/bin;


  this.prepareBins = function(binsize){
  preparedData = {};
  // console.log(binsize);

}
  histData = prepareBins(10);

  var margin = {
    top: 20,
    right: 20,
    bottom: 30,
    left: 60
  },
  width = Math.max(200,w) - margin.left - margin.right,
  height = Math.max(200,h) - margin.top - margin.bottom;
  pad = -70;



}

